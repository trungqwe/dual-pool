# Phase 2 — xác minh provenance production CLIProxyAPI v7.3.8

## Trạng thái

- Run ID: `phase-2-v7.3.8-verification`
- Branch: `phase-2/verify-v7.3.8-production-provenance`
- Start và current HEAD: `a0b1573346007ad08a9ebe7012167bffa79fbd99`
- Authoritative upstream: `phase-2/upstream-lifecycle` vẫn ở `a0b1573346007ad08a9ebe7012167bffa79fbd99`
- Commit/push/Source CI: chưa thực hiện; final snapshot gates PASS, sẵn sàng tạo commit scoped.
- Kết luận: implementation và test gates PASS tại worktree; release evidence chưa được delivery để auditor độc lập đọc. Không gọi đây là independent acceptance.

## Mục tiêu và ranh giới

Xác minh độc lập release v7.3.8 từ GitHub upstream chính thức, kiểm tra tương thích adapter và thêm receipt production bất biến chỉ khi các gate provenance/compatibility đạt. Slice này không staging/install/promote candidate, không đổi `upstream.lock`, không chạy OAuth hay gửi provider traffic.

## Provenance và compatibility

Tag v7.3.7 (`b773607e3e7756dc6020a291825e4eb08899595a`) là tổ tiên của v7.3.8 (`c93978c4ea2e908255a2a06c37599fda3651554a`); compare có 26 commits. Release `391791121` được phát hành `2026-09-18T21:17:57Z`, không draft/prerelease. Asset `573477067` có archive SHA-256 `5e3278ac9b57d16df503fd845827a6fdb57ec241f102b35899788287eb431351`, khớp GitHub asset digest, `checksums.txt` và hash local.

Archive có 5 entry, không traversal/absolute/duplicate/symlink-like entry. Executable là PE AMD64 regular file `cli-proxy-api.exe`, SHA-256 `479da2fb56eb3db11a76e19adeb2e10c2a4069a512ab5e3933ac4c50628360fd`. Lệnh `-h` báo v7.3.8 và commit `c93978c4`, là prefix hợp lệ của tag SHA. Source diff config ghi nhận optional `use-max-completion-tokens`, mặc định false, cùng các thay đổi khác không làm đổi semantics của adapter fields. Ma trận field/source đầy đủ nằm trong `evidence/phase-2-v7.3.8-verification/config-compatibility.json` và `source-config-diff.json`.

Disposable binary dùng đúng schema adapter, chỉ thay port bằng port loopback TEMP được chọn động. Startup, health, client auth, management auth và shutdown đều PASS trong harness trước đó; không gửi request generation, provider traffic bằng không. Catalog production hiện có v7.3.7/v7.3.8; v7.3.9/v7.3.12 bị từ chối. Không có installed slot v7.3.8 nên updater preflight từ chối trước lifecycle mutation. `upstream.lock` không đổi và stager vẫn chỉ nhận current pin.

## Sửa hiệu năng fixture BCrypt

Timeout trước đó ở `go test ./internal/cliproxyconfig -count=50`, trong `TestFaultRecoveryMatrix` tại `bcrypt.CompareHashAndPassword`, được phân loại là **resolved acceptance-harness performance issue**, không phải blocker correctness/security của sản phẩm. Nguyên nhân là `bcrypt.DefaultCost` nhân với 11 fault points và cả lượt fault/recovery trong mỗi iteration.

`Generator` giữ policy private theo instance; `New` luôn chọn `bcrypt.DefaultCost`. Test fixture TEMP đặt `bcrypt.MinCost` trước khi sử dụng generator, nhưng vẫn dùng bcrypt generation/comparison thật. `validateConfigWithCost` kiểm tra cả cost lẫn password; default validation từ chối hash MinCost. Không thêm setter/config/env/global hook sản xuất, không bỏ xác thực.

## Kết quả kiểm thử

Focused chẩn đoán `FaultRecoveryMatrix|SubprocessCrashRecovery|Bcrypt|Render|PairGeneration -count=20` PASS trong 47,206 giây. Lệnh gate gốc chính xác `go test ./internal/cliproxyconfig -count=50` PASS với Go timeout mặc định trong 127,210 giây.

Toàn bộ 7 package stress `-count=50` và 7 package race `-race -count=10` PASS. Full `go fmt ./...`, `git diff --check`, `go mod verify`, `go vet ./...`, `go test -count=1 ./...`, `go test -race -count=1 ./...`, Windows AMD64 build, Node 55/55, `node scripts/phase0-upstream-lock.cjs`, JSON 205 file, 27 local docs links trong handoff/report và secret/privacy scan 21 file (0 matches, positive control PASS) đều PASS. Durations theo package được ghi trong `evidence/phase-2-v7.3.8-verification/test-result.json`.

`go fmt ./...` đã chạy và thoát 0; formatter làm status báo rộng do metadata trên Windows. Các file ngoài phạm vi được checkout lại từ index; final diff chỉ còn file slice. Tám Go files slice được gofmt và `git diff --check` PASS.

## Cleanup và delivery gate

Containment snapshot PASS: PID `19432` không còn chạy; port `60422` không có listener. TEMP root và executable build tạm nằm dưới system TEMP, ngoài repository, Dual Pool product root và các user config roots đã kiểm tra. Worktree/repository không có `.exe`, `.zip`, candidate config, synthetic runtime material hay auth files; không có forbidden staged path; `DualPool/bin` không có v7.3.8 match. Bốn TEMP auth directories rỗng. Client/management keys còn trong TEMP chỉ được tạo cho verification, không đọc từ `secretstore`/WinCred/real credentials và không thể xác thực provider account; không ghi raw value vào evidence. Không có candidate slot nào được tạo bởi run này; không có production-root mutation, credential/OAuth access hay provider traffic. Remote authoritative branch và `upstream.lock` đều giữ nguyên SHA đã nêu.

**TEMP cleanup status: `POLICY_BLOCKED_NONREPO_TEMP_RESIDUE`.** Đã yêu cầu xóa artifact TEMP; automatic approval policy từ chối. Không thử bypass hay cơ chế xóa thay thế. Residue chỉ ở disposable system TEMP, không nằm trong repo/product root và không phải provenance artifact. Do owner xác nhận policy denial không chặn delivery khi containment đạt, repository delivery-ready là true; không tuyên bố cleanup PASS hay file đã bị xóa. `security-gate.json` và `test-result.json` giữ nguyên phân loại này; residual không được commit.

Các gate vẫn OPEN: trusted candidate staging/install; production Smoke; real promotion/rollback; retained executable handle; pathname TOCTOU; subprocess/OS/power-loss durability; updater CLI. Release receipt/catalog và kết quả compatibility hiện là bằng chứng cục bộ chưa được push, nên auditor chưa thể kiểm tra độc lập trên GitHub.

## Bước kế tiếp duy nhất

Tạo commit scoped từ các file đã review, kiểm tra staged paths để loại mọi binary/secret/TEMP artifact, push thường (không force) đúng branch này, đợi exact-SHA Source CI `completed/success`, rồi xác nhận `phase-2/upstream-lifecycle` không đổi. Chỉ sau delivery mới giao owner audit; không bắt đầu candidate staging trong slice này.

## Cập nhật CI exact-SHA

Push đầu tiên `918722fe91995436a9ffd24b8684ca210748f0e9`, run `35702682125`, thất bại riêng ở `Go tests`: `TestVerifiedV738ReceiptRejectsTampering` dùng mutation fixture phụ thuộc LF (`strings.Replace` trên mẫu có xuống dòng), nên Windows CRLF checkout không sửa được receipt; parser nhận nguyên receipt và test phát hiện mutation bị bỏ sót. Các bước format/module/vet PASS; race, pinned integration, build và Node bị skip. Production parser không phải nguyên nhân.

Test đã được sửa để tạo fixture mutation độc lập line ending và kiểm tra cả LF/CRLF; upstream parser/trust policy không đổi. `upstreamcatalog -count=50`, `-race -count=10`, `go mod verify`, `go vet ./...` và `go test -count=1 ./...` PASS sau sửa. Exact-SHA Source CI của commit sửa sẽ là delivery gate tiếp theo.
